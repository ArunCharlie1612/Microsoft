# Minimal dependencies to run BreachSim locally in stub/demo mode (no Azure cloud).
# All Azure / OpenAI / semantic-kernel imports in the code are lazy and only trigger
# when cloud endpoints are configured, so they are NOT needed to try the UI locally.
# Use this on Python 3.13+ where semantic-kernel/azure pins from requirements.txt fail.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.27.2
sse-starlette==2.1.3

# Tooling (tests + lint)
ruff==0.6.9
pytest==8.3.3
pytest-asyncio==0.24.0
